{
	"name": "vitest-bats",
	"build": {
		"dockerfile": "Dockerfile",
		"args": {
			"TZ": "${localEnv:TZ:America/New_York}",
			"GIT_DELTA_VERSION": "0.18.2",
			"ZSH_IN_DOCKER_VERSION": "1.2.0",
			"PNPM_VERSION": "10.33.0",
			"BIOME_VERSION": "2.4.9"
		}
	},
	"runArgs": ["--cap-add=NET_ADMIN", "--cap-add=NET_RAW", "--cap-add=SYS_PTRACE", "--security-opt=apparmor:unconfined"],
	"customizations": {
		"vscode": {
			"extensions": [
				"anthropic.claude-code",
				"bierner.markdown-footnotes",
				"bierner.markdown-preview-github-styles",
				"bierner.markdown-yaml-preamble",
				"biomejs.biome",
				"davidanson.vscode-markdownlint",
				"eamodio.gitlens",
				"github.copilot-chat",
				"github.vscode-github-actions",
				"github.vscode-pull-request-github",
				"mikestead.dotenv",
				"ms-vscode.live-server",
				"ms-vscode.vscode-speech",
				"ms-vscode.vscode-typescript-next",
				"redhat.vscode-yaml",
				"tombi-toml.tombi",
				"tombonnike.vscode-status-bar-format-toggle",
				"typescriptteam.native-preview",
				"unifiedjs.vscode-mdx",
				"vercel.turbo-vsc",
				"vitest.explorer",
				"vscode.vscode-typescript-next",
				"yoavbls.pretty-ts-errors"
			],
			"settings": {
				"editor.formatOnSave": true,
				"editor.defaultFormatter": "biomejs.biome",
				"editor.codeActionsOnSave": {
					"source.fixAll.biome": "explicit",
					"source.fixAll.markdownlint": "explicit",
					"source.organizeImports.biome": "explicit"
				},
				"biome.enabled": true,
				"biome.requireConfiguration": true,
				"eslint.enable": false,
				"prettier.enable": false,
				"terminal.integrated.defaultProfile.linux": "zsh",
				"terminal.integrated.profiles.linux": {
					"bash": {
						"path": "bash",
						"icon": "terminal-bash"
					},
					"zsh": {
						"path": "zsh"
					}
				}
			}
		}
	},
	"remoteUser": "node",
	"mounts": [
		"source=claude-code-bashhistory-${devcontainerId},target=/commandhistory,type=volume",
		"source=claude-code-config-${devcontainerId},target=/home/node/.claude,type=volume",
		"source=pnpm-store-${devcontainerId},target=/home/node/.local/share/pnpm,type=volume",
		"source=node-modules-${devcontainerId},target=/workspaces/vitest-bats/node_modules,type=volume",
		"source=pkg-node-modules-${devcontainerId},target=/workspaces/vitest-bats/package/node_modules,type=volume",
		"source=vscode-server-${devcontainerId},target=/home/node/.vscode-server,type=volume"
	],
	"containerEnv": {
		"NODE_OPTIONS": "--max-old-space-size=4096",
		"CLAUDE_CONFIG_DIR": "/home/node/.claude",
		"COREPACK_ENABLE_DOWNLOAD_PROMPT": "0",
		"npm_config_store_dir": "/home/node/.local/share/pnpm/store",
		"POWERLEVEL9K_DISABLE_GITSTATUS": "true"
	},
	"workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
	"postCreateCommand": "bash .devcontainer/postCreate.sh",
	"postStartCommand": "if [ \"${CODESPACES:-}\" = \"true\" ]; then echo 'Codespaces detected, skipping firewall'; else sudo /usr/local/bin/init-firewall.sh; fi",
	"waitFor": "postStartCommand"
}
